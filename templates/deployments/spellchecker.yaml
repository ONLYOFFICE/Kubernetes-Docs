apiVersion: apps/v1
kind: Deployment
metadata:
  name: spellchecker
  labels:
    app: spellchecker
spec:
  replicas: {{ .Values.spellchecker.replicas }}
  selector:
    matchLabels:
      app: spellchecker
  template:
    metadata:
      labels:
        app: spellchecker
    spec:
      volumes:
        - name: ds-license
          secret:
            secretName: license
      containers:
        - name: spellchecker
          image: {{ .Values.spellchecker.containerImage }}
          envFrom:
          - secretRef:
              name: jwt
          - secretRef:
              name: dbsecret
          - configMapRef:
              name: documentserver
          ports:
            - containerPort: 8080 
